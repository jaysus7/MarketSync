# Phase 4 Batch 1

Canonical dark canvas is **#121318** (`--ms-dark-canvas`).
`#0B1220` remains `--ms-blue-950` / deep ink only.

Shared pulse primitives now use `ms-c` + `ms-btn` (no local white cards).
This is the first route-family pass: Pulse list rows on every department that calls pulseRow/pulseCard.

PASS still 0 until each route is recaptured at 95+.
